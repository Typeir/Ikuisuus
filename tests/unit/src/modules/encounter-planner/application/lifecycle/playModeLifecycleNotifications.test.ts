/**
 * @fileoverview Unit tests for playModeLifecycleNotifications
 * @module tests/unit/src/modules/encounter-planner/application/lifecycle/playModeLifecycleNotifications
 * @version 1.0.0
 * @author Typeir
 */

import * as Module from '@/modules/encounter-planner/application/lifecycle/playModeLifecycleNotifications';
import { describe, expect, it } from 'vitest';

describe('playModeLifecycleNotifications module', () => {
  it('should export module', () => {
    expect(Module).toBeDefined();
  });
});
