/**
 * @fileoverview Unit tests for playModeTurnFlow
 * @module tests/unit/src/modules/encounter-planner/application/lifecycle/playModeTurnFlow.test
 * @version 1.0.0
 * @author Typeir
 */

import * as Module from '@/modules/encounter-planner/application/lifecycle/playModeTurnFlow';
import { describe, expect, it } from 'vitest';

describe('playModeTurnFlow module', () => {
  it('should export module', () => {
    expect(Module).toBeDefined();
  });
});
