/**
 * @fileoverview Layout constants for the GenericCombobox dropdown positioner.
 *
 * @module modules/encounter-planner/presentation/comboboxes/comboboxConstants
 * @version 1.0.0
 * @author Typeir
 * @since 1.0.0
 */

/** Vertical gap between the input bottom edge and dropdown top. */
export const OFFSET_Y = 4;

/** Minimum width in pixels for the dropdown panel. */
export const DROPDOWN_MIN_WIDTH = 180;

/** Maximum height in pixels for the dropdown panel before scrolling. */
export const DROPDOWN_MAX_HEIGHT = 300;

/** Minimum gap in pixels between dropdown edge and viewport edge. */
export const VIEWPORT_MARGIN = 8;
